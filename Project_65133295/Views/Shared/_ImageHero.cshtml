@model string
<div class="hero-image card-image-wrap rounded-large">
    <img src="@Model" alt="Hero Image" />
    <div class="absolute inset-0 flex items-end p-6">
        <div class="text-white">
            @* Placeholder for overlay content *@
        </div>
    </div>
</div>
